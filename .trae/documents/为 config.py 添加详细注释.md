我将为 `utils/config.py` 文件添加详细的中文注释，解释各个配置项的作用。

### 计划修改内容

**1. 模型配置部分**
- 注释说明各个阶段（Stage 1/2/3）使用的模型，以及用于总结、操作、求解（Medium/Strong）、评审和裁判的模型。

**2. API 配置部分**
- 注释说明 API 基础地址、密钥以及重试策略（最大重试次数、休眠时间等）。

**3. 生成流程配置部分**
- 注释说明最大生成轮数、日志和结果文件路径。
- 解释生成参数：每轮最大步数、最小跳数（用于控制复杂度）、难度强化尝试次数及模式、是否强制跨模态等。

**4. 图模式配置部分**
- 注释说明图模式开关、文档分块大小、是否要求不同来源、路径采样算法以及最大快捷边数。

我将直接在代码中添加注释，使其更易于理解和维护。